# Orders — Scribe (updated 26 Aug, twelfth sweep)

Round 2 merged: 8 false proposals → 2, zero missed sources, the
run-B structural finding (this corpus runs derived-from-model, the
product case runs document-into-model) is now on the record and in
front of the founder.

1. Round 3, registered first: the leading-paragraph defense (a
   numeric token opening an otherwise-prose line is a label, not a
   quantity).
2. Hold the hit-rate measurement until a document-fed pair exists —
   the lead is asking the founder for a real deal set (term sheet +
   model); do not force it on the wrong corpus.
3. Meanwhile: D5's registration (the unsourced-number finding) can
   proceed — your run-B population is exactly its raw material;
   flood measurement per the plan before anything reports.
