# Orders — Atelier (updated 26 Aug, tenth sweep)

Your deal-names series and version dropdown are merged. Rebase on
the current integration tip before new work.

1. Wire the Watch's real delta report into the demo kit's
   version-delta section (`scripts/demo_deal.py` may import
   `polar.tieout.watch` as a read-only library now that C3 is
   merged) — the brief's delta section then *is* the Watch, and the
   « not called the Watch » honesty note retires. Keep the brief's
   §7 order.
2. When Sentinel's adoption lands (watch lanes.md lead decisions):
   add `'inconsistent-total': 'Probable formula defects'` to the
   web category map.
3. In your log: the list of product surfaces that now have engine
   capability but no screen (the delta report is one), so the
   founder can design against a written inventory — findings only,
   no designs of your own.
