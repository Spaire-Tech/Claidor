import{clampBlock,clampLine}from"../../../shared/sand-text.js";import{isAnyChannelAvailable}from"../../../shared/cloud-agents-availability.js";export const SECRET_REQUEST_MAX_LABEL_LENGTH=120,SECRET_REQUEST_MAX_DESCRIPTION_LENGTH=400;
export const clampSecretLabel=(v:string):string=>clampLine(v,120);export const clampSecretDescription=(v:string):string=>clampBlock(v,400);
export const summarizeSecretRequest=(r:{label:string}):string=>`Requested a secret from the user securely: ${r.label}`;
// Channels are served since 25 September 2026 (host/extensions/channels/): the
// stored credential is read by the connector runtime within a few seconds and
// the connection opens, or its status says why not. With
// SAND_CHANNELS_SERVED=0 nothing reads it, and the ack says so.
export function buildSecretProvidedAck(r:{label:string;target:{kind:string}},env:NodeJS.ProcessEnv=process.env):string{const served=isAnyChannelAvailable(undefined,env);return[`[The user securely provided the requested secret: "${r.label}". It was written straight to its destination (${r.target.kind}); you never see the value and it is not in this conversation.]`,served?"Confirm to the user that it is stored, then continue. The channel connector reads it and links within a few seconds; a connection that needs a second credential (Slack's bot token, field \"botToken\") stays pending until that one is stored too, so ask for it the same way if you have not yet. The channel's live status ([connected], [pending] or [error] with its reason) is in the Channels section of your instructions on your next turn; report that, not a connection you have not seen.":"Confirm to the user that it is stored, then continue. It is stored for a messaging channel, and messaging channels are coming soon in Simeon, so no connection links from it yet; say so rather than reporting a connection."].join("\n")}
