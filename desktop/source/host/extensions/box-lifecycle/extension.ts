import { defineHostExtension } from "../../../internal/host-extensions.js"; import { GrokBotService } from "../../../packages/proto/generated/aiserver/v1/grok_bot_connect.js"; import { createSandCursorBackendClient } from "../../../shared/node/cursor-backend/cursor-inference.js"; import { HostExtensions } from "../extension-ids.generated.js"; import { isConnectServed } from "../../../shared/cloud-agents-availability.js"; import { BoxLifecycleService, type BoxLifecycleClient } from "./box-lifecycle-service.js";

// GrokBotService is Cursor's box broker; Simeon Labs' server does not serve it, and a local Docker box is updated from the Mac (Settings → Updates, or `docker restart simeon-box`). Until 25 September 2026 the host asked the unserved client at every start and told the agent "the backend may need to be updated" (F-140).
export const LOCAL_BOX_LIFECYCLE_REASON = "This computer runs in Docker on the user's Mac; it is updated from Simeon on the Mac (Settings → Updates), not from here.";
export const localBoxLifecycleClient: BoxLifecycleClient = { getSandBoxRunState: async () => ({ imageUpdateAvailable: false }), recreateSandBox: async () => ({ started: false, reason: LOCAL_BOX_LIFECYCLE_REASON }) };
interface AuthApi { getAccessToken(options: { backendUrl: string }): Promise<string>; getMachineId(): Promise<string>; }
export const boxLifecycleExtension = defineHostExtension({ id: HostExtensions.BoxLifecycle, dependencies: [HostExtensions.Auth], start: (context) => { const auth = context.deps[HostExtensions.Auth] as AuthApi; const client = isConnectServed() ? createSandCursorBackendClient(GrokBotService, { getAccessToken: auth.getAccessToken, getMachineId: auth.getMachineId }) as unknown as BoxLifecycleClient : localBoxLifecycleClient; return new BoxLifecycleService(client); } });
