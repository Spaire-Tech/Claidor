{
  "id": "ask-user-question",
  "name": "AskUserQuestion",
  "description": "Structured user confirmation tool for Caisra desktop.",
  "contracts": {
    "tools": [
      "AskUserQuestion",
      "ReactToMessage"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "callbackUrl": {
        "type": "string",
        "description": "Caisra HTTP callback URL for approval requests"
      },
      "secret": {
        "type": "string",
        "description": "Shared secret for callback authentication"
      },
      "reactUrl": {
        "type": "string",
        "description": "Caisra HTTP callback URL for ReactToMessage; absent, the tool is not offered"
      }
    }
  }
}
