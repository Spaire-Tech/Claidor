"""Planting harnesses — known defects, surgically placed, truth first.

Each harness in this package plants one registered round's defect
classes into real corpus files by XML surgery on the xlsx zip, so
every cell the plant does not touch keeps its cached value. The
ground truth is written before the engine ever sees the planted
file, and one planting run per host is the rule — no re-rolls.
"""
