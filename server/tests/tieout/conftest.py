"""Swens (model review for finance) is archived.

The engine under ``polar/tieout`` and these tests are kept in the tree
as a record; the routes are no longer mounted and the tests are not
collected. The last working state is the git tag ``swens-final``.
"""

collect_ignore_glob = ["*.py", "**/*.py"]
